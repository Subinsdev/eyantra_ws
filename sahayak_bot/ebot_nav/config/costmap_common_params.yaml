obstacle_range: 2.5			# tune
raytrace_range: 3.0			# tune
footprint: [[-0.12, -0.14], [-0.12, 0.14], [0.12, 0.14], [0.12, -0.14]]
inflation_radius: 0.55		# tune
transform_tolerance: 0.5	# tune

observation_sources: scan
scan:
  data_type: LaserScan
  topic: /ebot/laser/scan
  marking: true
  clearing: true

map_type: costmap
